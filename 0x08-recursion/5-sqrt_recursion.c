#include "main.h"

/**
 *_sqrt_recursion -  function that returns the natural square root of a number.
 *@n: integer parameter
 *Return: returns the square root of the n if n has a sqaure root
 *        returns -1 if n does not hvae a natural square root.
 */
int _sqrt_recursion(int n)
{
	int num;

	num = 0;
	
	if (n == 1)
	{
		return (1);
	}
	if (n < 0)
	{
		return (-1);
	}
}
